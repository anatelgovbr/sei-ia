# import logging
# from api_sei.exception_handling.exceptions import SQLAlchemyInsertError
# from sqlalchemy.orm import declarative_base
# from api_sei.db_models.models import UserDB
# from api_sei.pydantic_models.user import User

# from api_sei.db_models.db_instances import app_db
# from sqlalchemy.exc import SQLAlchemyError
# from psycopg2.errors import UniqueViolation

# logger = logging.getLogger(__name__)





# def insert_user(user: User):
#     if not app_db:
#         return

#     try:
#         new_user = UserDB(id_usuario=user.id_usuario, senha=user.senha, nome=user.nome)
#         app_db.add(new_user)

#         return True

#     except SQLAlchemyError as e:
#         if isinstance(e.orig, UniqueViolation):
#             logger.error(e, exc_info=True)
#             raise SQLAlchemyInsertError(f"Usuario {user.id_usuario} ja existe")

#         else:
#             logger.error(e, exc_info=True)
#             raise SQLAlchemyInsertError(f"SQL insert error : {str(e)}")

# def select_user(id_usuario) -> User:
#     try:
#         return app_db.get(model=UserDB, id=id_usuario)
#     except SQLAlchemyError as e:
#         logger.error(e, exc_info=True)
#         raise SQLAlchemyInsertError(f"SQL select error : {str(e)}")
